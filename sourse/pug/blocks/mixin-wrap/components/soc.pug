mixin item-sos(icon, a='#')
	+e.A.item(href= a target="_blank" data-aos="fade-up" data-aos-duration="700")
		if block
			block
		else
			+icon(icon)
//- social buttons
mixin soc()
	+b.soc
		if block
			block
		else
			each soc in social
				+item-sos(soc)

//- /social buttons 